# INSTALLATION:
In order to build and install this dns server, you can enter the following
commands:

cmake .
make
sudo make install


# Usage the following options are currently supported:
  --file [filename]: Mandatory option, where file corresponds to a zonefile
  -- ipv4